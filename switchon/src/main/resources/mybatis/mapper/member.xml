<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.switchon.diet.member.dao.IMemberDAO">

	<insert id="registMember" parameterType="com.switchon.diet.member.vo.MemberVO">
		INSERT INTO members(mem_id, mem_pw, mem_nm)
		VALUES(#{memId}, #{memPw}, #{memNm})
	</insert>
	
	<select id="loginMember" parameterType="MemberVO" resultType="MemberVO">
	
		SELECT mem_id
		     , mem_nm
		     , mem_pw
		FROM members
		WHERE use_yn = 'Y'
		AND   mem_id = #{memId}
	
	</select>
	
	<update id="updateMember" parameterType="com.switchon.diet.member.vo.MemberVO">
		
		UPDATE members
		SET mem_nm = #{memNm}
		WHERE mem_id = #{memId}
	
	</update>
	
	
	
	
</mapper>